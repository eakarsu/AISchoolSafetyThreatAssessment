name: governance-ci

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  governed-workflow:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Governed workflow tests
        run: node --test server/governance/*.test.cjs
      - name: JavaScript syntax
        run: |
          node --check server/index.js
          find server/governance -type f -name '*.js' -print0 | xargs -0 -n1 node --check
      - name: Launcher syntax and safety
        run: |
          bash -n start.sh
          ! grep -R -E 'process\.env\.JWT_SECRET[[:space:]]*\|\|' server --include='*.js'
          ! grep -R -E 'rejectUnauthorized:[[:space:]]*false' server --include='*.js'
      - name: Additive migration boundary
        run: |
          grep -q governed_tenant_memberships server/migrations/001_governed_school_safety.sql
          grep -q 'append-only' server/migrations/001_governed_school_safety.sql
          ! grep -E 'DROP TABLE|TRUNCATE' server/migrations/001_governed_school_safety.sql
      - name: Credential and TLS boundary
        run: |
          ! grep -R -E "JWT_SECRET[[:space:]]*\|\||rejectUnauthorized:[[:space:]]*false" server --include='*.js'
